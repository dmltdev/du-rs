# du-rs

A starter project: re-implementing the Unix `du` CLI in Rust.

Personal learning exercise to get familiar with Rust by rebuilding a tool I already understand. Not intended as a drop-in replacement for `du(1)`.

## Build

```sh
cargo build --release
```

## Run

```sh
cargo run -- <path>
```
